/**
 * Plain constants shared by both client and server code. Keep this file free
 * of any server-only imports (e.g. Supabase) since it's imported from
 * "use client" components too.
 */

/** The one client with a branded login intro video (see MarksmenIntroOverlay). */
export const MARKSMEN_SLUG = "dmv-marksmen";
